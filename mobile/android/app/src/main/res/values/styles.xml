<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Cold-start launch theme: shown via the activity's window background before
         MainActivity.onCreate swaps back to AppTheme (see setTheme call there).
         Plain color, not a logo image — splashscreen_logo.png was a leftover
         placeholder grid/target graphic, not real branding (visible as
         unwanted grid lines on launch); swap in a real logo drawable here
         later if/when one exists. -->
    <style name="Theme.App.SplashScreen" parent="AppTheme">
        <item name="android:windowBackground">@color/splashscreen_background</item>
    </style>

</resources>
