<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- react-native-image-picker: gallery access on older Android; on 33+ the
         system photo picker needs no runtime permission at all. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" tools:replace="android:maxSdkVersion" />
    <!-- @react-native-firebase/messaging: required on Android 13+ to display
         the order-ready visible notification (specs/009-order-fcm-push-notifications). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- BatteryOptimizationModule: one-tap prompt so MIUI/OEM power management
         doesn't silently delay FCM delivery for specs/014's mobile-verification
         push fallback (confirmed on-device via MIUI's SmartPower throttling +
         an FcmRetry alarm in logcat). ACCEPTED RISK: Google Play restricts this
         permission to a narrow set of app categories (alarms, VPNs, device
         management) that a food-ordering app does not clearly fit; using it
         anyway risks Play Store rejection or later removal. Explicitly
         requested and accepted by the project owner after being told this. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true"
      android:enableOnBackInvokedCallback="false">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.App.SplashScreen"
        android:screenOrientation="portrait"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
